# PlayerPulse — top-level dependencies, pinned to tested versions.
# Pins match the trained model so joblib.load works across platforms.
# Full transitive lock is in requirements-lock.txt.
streamlit==1.58.0
plotly==6.8.0
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.18.0
shap==0.52.0
joblib==1.5.3
matplotlib==3.11.0
seaborn==0.13.2
pytest==9.1.1
